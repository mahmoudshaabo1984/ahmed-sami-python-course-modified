


# This command uses the **pip** package manager to **install** the `pyinstaller
# pip install pyinstaller

# This command uses the installed pyinstaller library to turn the Python script 51p4.py** into a single, standalone **.exe executable file.
# pyinstaller --onefile 51p4.py


# This command, used on Windows, opens a File Explorer window for the current directory
# explorer .